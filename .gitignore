# Metadados do macOS
.DS_Store

# Dependências
node_modules/

# Build
dist/
*.tsbuildinfo

# Cache do índice do roteador (denso, persistido em disco)
.cache/
# Artefatos do índice, caso o cacheDir seja apontado para outro lugar
index-*.bin
index-*.meta.json

# Espaço de rascunho / experimentos
.scratch/

# Pesos do modelo de embedding (MiniLM) são versionados via Git LFS (ver .gitattributes),
# para o repo funcionar offline sem baixar do HF Hub. NÃO ignoramos chat/models/.

# Logs
*.log

# Variáveis de ambiente (podem conter credenciais). Versione apenas .env.example.
.env
.env.local
.env.*.local
!.env.example
